---
import { partnerNames, studio, whatsappUrl } from "../config"

/**
 * The footer from the Figma design: the studio and its architects on the left,
 * how to reach them on the right, and the year centred underneath. It shares
 * the last screen with the final row of photographs.
 */

const year = new Date().getFullYear()
---

<footer class="footer">
  <div class="columns">
    <section>
      <h2>{studio.name}</h2>
      <p class="label">Arquitetas</p>
      <ul role="list">
        {partnerNames.map((name) => <li>{name}</li>)}
      </ul>
    </section>

    <section>
      <h2>Contato</h2>
      <ul role="list">
        <li><a href={`mailto:${studio.email}`}>{studio.email}</a></li>
        <li>
          <a href={studio.instagram} rel="noopener">{studio.instagramHandle}</a>
        </li>
        <li><a href={whatsappUrl} rel="noopener">chame pelo WhatsApp</a></li>
      </ul>
    </section>
  </div>

  <p class="colophon">© {year} {studio.name}</p>
</footer>

<style>
  .footer {
    display: flex;
    flex-direction: column;
    gap: var(--space-5);
    padding: var(--space-6) var(--edge) var(--space-5);
    font-size: var(--text-ui);
    line-height: 1.45;
  }

  .columns {
    display: grid;
    gap: var(--space-5);
  }

  h2 {
    font-size: inherit;
    font-weight: 600;
  }

  .label {
    margin-block-start: 1.35em;
  }

  ul {
    margin: 1.35em 0 0;
    padding: 0;
  }

  /* The architects sit straight under the word that introduces them. */
  .label + ul {
    margin-block-start: 0;
  }

  section a {
    position: relative;
  }

  /* The underline draws in on hover, as it does in the menu. */
  section a::after {
    content: "";
    position: absolute;
    inset-block-end: -0.1em;
    inset-inline: 0;
    block-size: 1px;
    background: currentColor;
    transform: scaleX(0);
    transform-origin: left;
    transition: transform var(--motion-fast) var(--ease-out-soft);
  }

  section a:hover::after,
  section a:focus-visible::after {
    transform: scaleX(1);
  }

  .colophon {
    /* The base rule caps paragraphs for readability; this line is centred on
       the photographs, so it needs the full width. */
    max-inline-size: none;
    color: var(--color-mute);
    /* Four fifths of the interface size, as in the design. */
    font-size: calc(var(--text-ui) * 0.8);
    font-weight: 500;
    text-align: center;
  }

  @media (min-width: 60rem) {
    .footer {
      margin-inline-start: var(--rail-width);
      /* A clear breath between the last photographs and the footer. */
      padding: var(--space-7) var(--edge) var(--edge) 0;
    }

    .columns {
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: var(--edge);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    section a::after {
      transition: none;
    }
  }
</style>
